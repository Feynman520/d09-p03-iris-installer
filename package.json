{
  "name": "iris-installer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/",
    "build": "node build/build.mjs",
    "verify": "node verify/static.mjs && node verify/reproduce.mjs"
  }
}
