{
  "name": "iris-installer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/**/*.test.mjs",
    "build": "node build/build.mjs",
    "verify": "node verify/static.mjs && node verify/reproduce.mjs && node verify/extract-matrix.mjs && node verify/offline.mjs --build && node verify/offline.mjs && node verify/e2e.mjs"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.1134.0",
    "@aws-sdk/lib-storage": "^3.1134.0"
  }
}
