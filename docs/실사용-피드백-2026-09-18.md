# 실사용 피드백 인수 — 집 데스크탑 2.0.11 설치 (2026-09-18 00:40)

**결과: 2.0.11 실제 PC 설치 성공(⑦ 계정 연결 통과, 완료 화면 도달).** 이후 사용 중 발견된 문제 5건 + 코덱스 세션의 TeamClaude 정밀진단(TC-01~TC-11)을
**담당 프로젝트별로** 나눈다. 원문 = P02 `state\uploads\2026-09-17T15-46-52-…-TeamClaude-정밀진단.md`.

## 1. 담당별 분류

| # | 증상(사용자 원문 요지) | 담당 | 처리 |
|---|---|---|---|
| F-1 | 코덱스 계정만 있으면 세션 제목을 클로드 하이쿠로 지으려 함 → 코덱스만 있어도 제목을 지어야 함 | **P02 Face** | 인수 |
| F-2 | 등록된 계정만 세션 생성 때 선택 가능하게(코덱스만 있으면 클로드 비활성) | **P02 Face** | 인수 |
| F-3 | 첫 설치 뒤 대화창의 노란(허락) 단추를 눌러도 선택이 안 됨(터미널은 됨) | **P02 Face** | 인수 — 첫 설치 직후 재현 조건(권한 카드 ↔ CLI 응답) |
| F-4 | 첫 설치 뒤 권한을 최대로 주고 싶은데 설정이 안 됨 → 터미널에서 강제 변경 | **P02 Face**(설정 UI) + **P03**(설치기가 초기 권한 모드를 물을지 — 설계 결정) | 인수 + 설계 질문 |
| TC-01 | 시작 보조가 3456 에 200 이 오면 "우리 중계기"로 간주 — 실행본·설정 미확인 | **P03**(`lib/proxy.mjs`) + dash(`ensure-proxy.mjs`) | **설계 결정 필요**(2절) |
| TC-02 | 완료 판정이 파일 계정 수로 — 실행 중계기 계정과 불일치; `relayImport` 다시읽기 결과 미검사 | **P03**(`lib/online.mjs`·`lib/login.mjs`) | 2.0.13 에서 고침 |
| TC-03·04·05·11 | 대시보드 표시(공급자 누락→CODEX 표시, Fable 막대 필드 불일치, 미확인→꺼짐 표시, enabled/status 필드) | **dash**(`<설치폴더>\_agent\claude\tools\teamclaude-dash`, P03 이 `dash` 부품으로 동봉) | 인수 |
| TC-06·08·10 | 대시보드의 로그인·리로드·전환 버튼이 전역 npm 실행본을 고정 호출, 결과 미감시 | **dash** | 인수 |
| TC-07 | Windows `start "URL"`(첫 따옴표 인수 = 창 제목) → 브라우저가 안 열림. 전역본·동봉본 모두 | **P03**(`patches/teamclaude/rules.json` 에 규칙 추가) | 2.0.13 에서 고침 |
| TC-09 | 시작·계정 관리·설정 경로 불일치; Face 세션 생성 때 `TEAMCLAUDE_CONFIG` 미복원 | **P02 Face**(`daemon/sessions.mjs`) + dash | 인수 |

### 1-2. 추가(02:30) — IRIS 창 「업데이트」가 설치 패키지를 거절

집 데스크탑(2.0.11 설치)에서 IRIS 창의 업데이트를 누르자 두 번 연속 거절됐다.

| # | 현상 | 원인 | 담당 | 처리 |
|---|---|---|---|---|
| F-5 | 「지난 내려받기 실패: 패키지: .sha256.sig 첨부가 없습니다」 | 2.0.4~2.0.13 을 수동 릴리스하면서 공식 서명(`<zip>.sha256.sig`)을 빠뜨림 | **P03** | 2.0.13 에 서명 첨부(GitHub·미러), 절차 고정(AGENTS.md) — 완료 |
| F-6 | 「지난 내려받기 실패: 패키지: inflate exceeded declared size: installer/ui/.gitkeep」 | **P02 `daemon/zip.mjs` `inflateEntry`**: `zlib.inflateRawSync(raw, { maxOutputLength: e.usize })` — Node 는 `maxOutputLength` 가 **1 이상**이어야 하므로 크기 0 인 항목(bsdtar 는 빈 파일도 deflate 해 csize 2 · usize 0)에서 `ERR_OUT_OF_RANGE`(RangeError) → 업데이트기가 "exceeded declared size" 로 보고. **1.x 부터 모든 판이 이 빈 파일을 실었으므로 업데이트기는 한 번도 패키지를 적용한 적이 없다** | **P02 Face**(`daemon/zip.mjs`) + **P03**(패키지) | P03 2.0.14: 빈 파일 제거 + 정적 검사 ⑲(0 바이트 파일 금지). **P02 인수: `maxOutputLength: Math.max(1, e.usize)`** 한 줄(빈 파일이 정당하게 필요한 판이 나오면 그때까지 P03 이 막힌다) |

| F-7 | 「확인 실패: 패키지: release has no matching zip」(03:20) — 2.0.14 릴리스에 zip 첨부가 아직 없을 때 | 업데이트기 `fetchLatest` 가 **GitHub 릴리스 첨부 목록**에서만 zip 을 찾는다(미러는 내려받기 대체용). GitHub 첨부 서버가 383 MB 업로드를 HTTP 500 「Error saving asset」·연결 끊김으로 반복 거절한 밤(09-18 02:35~)에는 미러·홈페이지에 판이 있어도 업데이트가 시작조차 못 한다 | **P02 Face**(`daemon/update.mjs`) | 인수: 첨부 목록이 zip 없이 오면 **미러의 `<asset>.sha256`(HEAD 200)로 존재를 확인해 미러 주소를 zip 으로 삼는** 폴백(이름 규칙 `IRIS-Setup_v<판>_<날짜>.zip` 은 `.sha256` 텍스트 2열에 있다). P03 은 그동안 첨부를 붙을 때까지 재시도 |

### 1-3. 추가(13:00) — 네 번째 실제 PC: 설치 11/11 통과, 대시보드에 계정은 보이는데 세션 활동 0

사용자 답변: 대시보드 계정 = **ChatGPT/코덱스만** · 세션 안에서 claude.ai 로그인을 **따로 했음** · 세션 종류는 둘 다/모름.

| # | 현상 | 원인 | 담당 | 처리 |
|---|---|---|---|---|
| F-8 | 클로드 세션이 되는데 대시보드에 활동이 없음 | 중계기에 클로드 계정 0 → TeamClaude 는 클로드 요청을 **합성 429 로 막지** 직행시키지 않는다. 그런데 세션이 됐다 = 그 세션엔 `ANTHROPIC_BASE_URL` 이 없었다(직행). 검사 1~11 은 배선을 안 봄 | **P03**(2.0.16 검사 12) + **P02 Face** | P03: 검사 12 「중계기 경유」(배선 4곳 · 계정 수 · 살아 있는 요청 1건). **P02 인수:** Face 가 세션에 넘기는 env(`applyReceiptEnv`)를 자가 진단해 첫 세션 전에 검사 12 와 같은 실측을 하고, 클로드 계정 0 이면 세션을 열기 전에 "⑦ 에서 claude.ai 로그인" 카드를 띄운다 |
| F-9 | **코덱스 세션은 어떤 설치 길에서도 TeamClaude 를 거치지 않는다** | TeamClaude 1.1.16 은 코덱스를 **MITM 전달 프록시 모드**(`HTTPS_PROXY=http://127.0.0.1:3456` + 자체 CA 신뢰, `chatgpt.com` CONNECT 가로채기)에서만 중계한다. IRIS 는 base-URL 모드(`ANTHROPIC_BASE_URL`)만 배선하므로 코덱스 트래픽은 중계기를 모른다. 이 개발 PC 도 같다(CA 파일 없음 = MITM 사용 이력 없음) | **설계 결정**(3절) | 코덱스까지 대시보드에 잡히게 하려면 세션 env 에 `HTTPS_PROXY/HTTP_PROXY/NO_PROXY` + CA 신뢰가 필요. 코덱스 CLI 는 Rust 라 `NODE_EXTRA_CA_CERTS` 가 아니라 `SSL_CERT_FILE` 또는 사용자 인증서 저장소가 필요 — **실험으로 확인해야 함** |

## 2. 설계 결정이 필요한 것 — "이미 다른 TeamClaude 가 3456 을 쓰는 PC"

관측: 전역 npm TeamClaude 1.2.1(기존 Claude 계정 5개)이 3456 을 점유 → 설치기는 200 만 보고 "연결됨"으로 기록 → 대시보드는 동봉본(1.1.16+IRIS 패치) 기준
화면이라 전역본 응답과 어긋남, Codex 는 TeamClaude 를 경유하지 않음.

선택지:
- **A(권고) IRIS 동봉 중계기로 통일** — 설치기가 3456 의 주인이 동봉 node·동봉 entry 가 아니면 멈추고 알린다: "다른 TeamClaude 가 3456 을 쓰고 있습니다.
  IRIS 것으로 바꾸려면 [기존 계정 가져와서 교체] / 그대로 두려면 [건너뛰기(코덱스 연결·대시보드 제한)]". 교체 = 기존 설정 파일의 계정을 IRIS 설정으로
  **복사(삭제 없음)** → 전역본을 `stop` → 동봉본 `start`. 되돌리기 가능.
- B 현행(200 이면 재사용) 유지 + 화면에 "다른 실행본" 경고만.
- C 포트를 나눔(IRIS 는 다른 포트) — Face·심(shim)·`ANTHROPIC_BASE_URL` 전부 3456 고정이라 큰 변경.

## 2-2. 설계 결정 — "코덱스 세션도 중계기를 거치게" (2026-09-18 13:05 사용자 요구: claude만/codex만/둘 다 어느 길이든 대시보드에 잡히고 경유해야 한다)

사실: TeamClaude 는 코덱스를 MITM 모드에서만 중계한다(F-9). 선택지:
- **A(권고) MITM 모드로 전환** — 세션 env 를 `HTTPS_PROXY=HTTP_PROXY=http://127.0.0.1:3456`·`NO_PROXY=localhost,127.0.0.1,::1`·클로드 `NODE_EXTRA_CA_CERTS=<CA>`·코덱스 `SSL_CERT_FILE=<CA>`(또는 사용자 인증서 저장소 등록)로 바꾸고 `ANTHROPIC_BASE_URL` 은 뺀다(두 모드는 겹치면 안 됨). 중계기 시작 때 CA 를 만들게 하고(`ensureCerts`), 검사 12 를 "CONNECT 가로채기 실측"(`www.example.org` 내장 시험 호스트)으로 넓힌다. 위험: 로컬 CA 를 신뢰시키는 일이라 사용자에게 설명이 필요하고, 코덱스 CLI 의 CA 신뢰 방식은 **이 PC 에서 임시 중계기(3499)로 실험해 확정**해야 한다.
- B 현행(클로드만 경유) + 대시보드에 "코덱스는 경유 안 함" 표시 — 사용자 요구에 못 미침.

### 2-2-1. 실험 A 결과(2026-09-18 13:05~13:17, 사용자 승인 "A 로 실험 시작") — **A 는 기술적으로 성립한다**

이 PC 에서 임시 TeamClaude 1.1.16 을 **3499 포트**(3456 은 건드리지 않음)·별도 설정 파일(코덱스 계정 3개만 복사)로 띄워 실측했다. 실험이 끝난 뒤 임시 실행본은 PID 로만 종료하고 토큰이 든 설정 사본은 지웠다.

| 단계 | 실측 | 뜻 |
|---|---|---|
| ① 중계기 기동 | `/teamclaude/status` 200, 계정 3(codex) | 임시 실행본 정상 |
| ② CA 생성 시점 | 기동 직후엔 `teamclaude-ca.pem` 없음 → **첫 CONNECT 요청 뒤 생성**(지연 생성) | 설치기가 CA 를 세션 env 에 넣으려면 **중계기 시작 뒤 시험 요청 1건을 보내 CA 를 만들어 두어야** 한다 |
| ③ 내장 시험 호스트 | `curl --proxy 3499 --cacert <CA> https://www.example.org/` → **200** + `{"teamclaude":"mitm-proxy-ok"}` | CONNECT 가로채기 동작. 검사 12 확장의 실측 방법으로 쓸 수 있다 |
| ④ 코덱스 CLI 0.154.0(Rust) | `HTTPS_PROXY=http://127.0.0.1:3499` + `SSL_CERT_FILE=<CA>` + `codex exec …` → 중계기가 **HTTP 429 「No account can serve this request: all 3 accounts are at their quota」** 를 돌려줌, 중계기 stderr 에 `MITM session error: read ECONNRESET` | **429 문구는 TeamClaude 의 것**(OpenAI 직행이면 이 문구가 나올 수 없음) → 코덱스 트래픽이 중계기를 **통과했다**. 429 자체는 그 3 계정의 한도 소진 때문이며 경로 실패가 아님. `SSL_CERT_FILE` 만으로 CA 신뢰가 되므로 **사용자 인증서 저장소 등록은 필요 없다** |

미확인(다음 실험 또는 구현 중 확인): ⓐ 클로드 코드(Node)가 같은 env(`HTTPS_PROXY` + `NODE_EXTRA_CA_CERTS`)에서 `ANTHROPIC_BASE_URL` 없이 3456 을 통과하는지 — TeamClaude 문서상 지원이나 이 PC 에선 실측 전 ⓑ 한도가 남은 코덱스 계정으로 200 까지 받는 것(429 로 경로는 증명됐으므로 필수는 아님).

## 3. 처리 순서(제안)

1. P03 2.0.13: TC-02(실행 중계기의 `/teamclaude/status` 계정 수로 판정, `relayImport` 결과 검사) · TC-07(`start "" "URL"` 패치 규칙) — 설계 결정 불필요.
2. 2절 결정 뒤 TC-01(주인 확인 + 안내/교체 흐름).
3. P02·dash 항목은 각 프로젝트 세션에서 이 문서를 읽고 진행(P03 은 md 인수만).
