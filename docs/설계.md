# IRIS 설치 패키지 설계 v1 (2026-09-10, 사용자 확정 조각 ①~⑥ 통합)

> 상태: 명세 초안 — 사용자 검토 대기. 확정되면 `docs\구현계획.md`로 이어진다.
> 만든 사람: 함세준 · 허가서: MIT · 패키지 이름: IRIS (2026-09-10 결정, P02 구현계획 v2.27과 동일)

## 0. 목표와 불변 원칙

**목표 한 줄.** 아무것도 설치되지 않은 보통 윈도우 PC를 가진 사람이 **zip 하나를 받아 두 번 클릭**하면, 사람만 할 수 있는 **구독 로그인 한 번**을 빼고 IRIS 체계 전체가 끝까지 설치된다. 그 뒤로는 IRIS-Face 아이콘만 누른다.

**왜 만드나.** 지금까지는 연수·책으로 터미널을 열어 에이전트를 설치하게 했다. 컴퓨터마다 윈도우 버전·winget 유무·실행 정책·기존 Node·백신·권한이 달라 같은 명령이 어떤 PC에서는 되고 어떤 PC에서는 안 됐다. 원인의 공통점은 "그 컴퓨터에 이미 있는 것에 의존했다"는 것이다. 이 패키지는 필요한 것을 전부 갖고 가서 그 의존을 없앤다.

**불변 원칙.**
1. **사용자 수동 단계 0.** 어디 들어가서 무엇을 내려받거나 명령을 치는 일이 없다. 예외는 브라우저 구독 로그인 하나.
2. **완전 동봉.** 설치기가 인터넷에 기대는 순간은 로그인(과 6-3의 Claude Code 예외)만. 나머지 부품은 zip 안에 있다.
3. **자급자족.** 컴퓨터에 이미 있는 Node·Python·Git·winget·관리자 권한에 의존하지 않는다. 동봉 런타임을 `_agent\shared\tools`에 두고 거기서만 실행한다.
4. **지우지 않는다.** 기존 폴더·기존 프로그램·사용자 자료를 삭제·덮어쓰지 않는다. 부품 교체는 `_agent\shared\tools` 안에서만, 이전 판은 `.prev`로 보존.
5. **재실행 안전.** 중간에 꺼져도 다시 실행하면 영수증을 읽고 끝난 단계는 건너뛴다.
6. **프로세스 종료는 PID로만.** 이름 기반 일괄 종료 금지(루트 규칙).
7. **비밀은 만지지 않는다.** 토큰은 TeamClaude가 자기 자리에 쓰고, 설치기·영수증·로그에는 들어가지 않는다.
8. **zip을 손으로 고치지 않는다.** 원본 수정 → 재빌드 → 검사소 통과 → 새 버전 이름.
9. **부품 잠금.** 모든 부품은 버전·SHA-256으로 잠근다. `latest`·`main`·무버전 `npx` 금지(세팅가이드 8-6절과 동일).
10. **TeamClaude 항상 경유.** 계정 수와 상관없이 에이전트는 TeamClaude 프록시(127.0.0.1:3456)를 거친다(사용자 결정 2026-09-10).

**정직한 한계(설치기가 첫 화면에서 미리 검사해 알린다).**
- Windows 10 1809 이상 또는 11, 64비트(32비트·Windows 7·8은 Node 24가 돌지 않음).
- 로그인 순간의 인터넷: `claude.ai`·`chatgpt.com`이 열려야 한다. 이를 막는 회사망은 방법이 없다.
- 디스크 여유 2GB.
- 그 외 관리자 권한·기존 Node·백신·실행 정책·PATH 문제는 자급자족으로 우회된다.

## 1. 결정 기록 (사용자 확정, 2026-09-10)

| 번호 | 쟁점 | 결정 | 근거 |
|---|---|---|---|
| D1 | 상자 안 구성 | 에이전트(Claude Code·Codex) + 세팅가이드 + IRIS-Face + TeamClaude 대시보드 + TeamClaude 프록시 | 지금 체계 전부를 한 상자로 |
| D2 | TeamClaude 포함 방식 | **항상 설치·항상 경유**(계정 1개여도) | "나중에 계정이 늘면 따로 설치해야 하나"를 없애기 위해. 선택 설치·잠자기 안은 폐기 |
| D3 | 설치 방식 | **자급자족형(포터블)** — Node·Python·Git·에이전트를 동봉하고 `_agent\shared\tools`에서만 실행 | 시스템 설치형(winget·공식 설치기)은 기존 고장 원인을 그대로 안고 감 |
| D4 | 부품 조달 | **완전 동봉**(약 450~500MB, 압축 300MB대) | 회사·학교망이 npm·GitHub를 막는 경우까지 성공시키기 위해 |
| D5 | 파일 형태 | `IRIS-설치.zip` 안의 `IRIS-설치.cmd` 두 번 클릭. 설치 화면은 브라우저의 로컬 HTML | 서명 없는 `.exe`는 SmartScreen·Smart App Control이 막음. `.cmd`는 App Control 정책의 대상은 아니지만, **⚠ 정정(2026-09-14 실측):** Smart App Control이 켜진 PC는 인터넷 출처 표식(MotW)이 붙은 `.cmd`·`.lnk` 등의 두 번 클릭(ShellExecute)을 "웹의 위험한 파일 확장자"로 막는다(실행 단추 없음). 표식은 zip에서 풀린 파일 전부에 번진다(탐색기·반디집 공통). 표식 없는 `.cmd`, `cmd /c`로 직접 실행, `powershell -File`의 표식 있는 `.ps1`(FullLanguage)은 통과. 대책 = 홈페이지·설치 안내에 "zip 차단 해제 후 풀기"와 "주소창 cmd" 두 길 안내. 근본 해결은 서명된 `.exe` 실행기(코드 서명 인증서 필요 — 미결) |
| D6 | 인수인계 | 설치기(브라우저 화면) → **IRIS-Face**로 넘김. 영수증 파일 + 최소 표지 + 가이드 v10 | Face를 설치기로 겸용하는 안은 닭과 달걀·베타 위험으로 폐기 |
| D7 | 구독 선택 | "어떤 구독이 있나요?" ☐ Claude ☐ ChatGPT. 둘 다면 Claude Code가 첫 세팅 주도 | 도구 이름보다 구독 이름을 사용자가 안다. Face 역할 분담(클로드=머리) |
| D8 | 로그인 시점 | 전부 설치기 단계에서 끝낸다 | Face로 넘어간 뒤 에이전트가 로그인을 요구하지 않게 |
| D9 | 사용자가 처음 보는 IRIS 화면 | IRIS-Face (설치기 탭은 1~2분 앞에만) | 터미널 거부감을 줄이는 것이 이 패키지의 존재 이유 |
| D10 | 설치기 포트 | 3460 | 3458=Face 데몬, 3459=Face 시험 데몬 |
| D11 | 이름·허가서 | IRIS · MIT | P02 v2.27 결정 계승 |
| D12 | npm 부품 포장 형식 (구현계획 자체 점검에서 추가) | `npm pack` tgz가 아니라 **`npm install -g --prefix` 결과 폴더를 zip** | tgz는 의존성이 없어 오프라인 설치가 안 됨. 설치 = 풀기만 |

## 2. 조각 ① — 패키지 내용물과 설치 후 구조

### 2-1. zip 안 (보내는 것)

```text
IRIS-설치.zip                      ← 전달 파일 1개 (압축 300MB대)
├ IRIS-설치.cmd                    ← 사용자가 두 번 클릭하는 유일한 파일 (UTF-8 + chcp 65001)
├ installer\
│  ├ bootstrap.ps1                 ← 1단: 무결성 검사 → Node 풀기 → 설치기 서버 켜기 (PowerShell 5.1, UTF-8 BOM)
│  ├ server.mjs                    ← 2단: 127.0.0.1:3460 웹 화면 서버 + 설치 작업 (동봉 Node)
│  ├ lib\manifest.mjs              ← 지문 계산·검증 (빌드 도구와 같은 파일)
│  └ ui\index.html                 ← 설치 화면 (단일 HTML, Face style.css의 색·글꼴 변수 블록 복사)
└ payload\
   ├ manifest.json                 ← 모든 부품의 이름·버전·SHA-256 (무결성 검사 기준)
   ├ node\      node-v24.x-win-x64.zip
   ├ agents\    claude-code-<ver>.tgz · codex-<ver>.tgz         ← npm pack 원본 (설치 = 풀기) ※ Claude Code는 6-3 참조
   ├ relay\     teamclaude-<ver>+iris.zip                        ← 로컬 패치 적용본(이 PC 라이브 설치본 기준 패치 파일 일체 — 2026-09-11 Task 6 판정, 09-07 ZIP 4파일 + 09-08 패치 포함)
   ├ face\      iris-face-<ver>.zip                              ← node_modules(Electron·node-pty·xterm) 포함
   │            iris-messenger-v<ver>.zip                        ← 메신저 모듈 서명 zip(1.4.0부터 기본 설치, GitHub 릴리스 원본 SHA 대조)
   ├ dash\      teamclaude-dash\
   ├ python\    python-3.12.x-embed-amd64.zip + PyYAML-<ver>.whl
   ├ git\       MinGit-2.54.x-64-bit.zip
   ├ guides\    세팅가이드(Claude 실행판)_v10.md · 세팅가이드(Codex 실행판)_v10.md
   └ licenses\  부품별 허가서 사본
```

`manifest.json`의 지문이 실제 파일과 다르면 설치기는 시작 전에 "파일이 손상됐습니다. 다시 받아 주세요"로 멈춘다.

### 2-2. 설치 후 (받는 사람의 컴퓨터) — 세팅가이드 6절 표준 구조를 그대로 따른다

```text
C:\<영혼 이름>\
├ AGENTS.md · CLAUDE.md               ← 가이드가 만든다
├ soul-state.json                      ← 설치기가 최소 표지로 먼저 씀 → 가이드가 채움
├ <영혼> Face.cmd                      ← 루트 소환기 (바탕화면 .lnk가 가리킴)
├ _setup-guides\ 두 판                  ← 에이전트가 읽는 계약서 (잠든 에이전트를 깨울 때도 필요)
└ _agent\
   ├ claude\  codex\                   ← CLAUDE_CONFIG_DIR · CODEX_HOME (가이드 2-2절)
   ├ setup\   package-receipt.json · package-install.log
   ├ shared\tools\  node\ python\ git\ claude\ codex\ teamclaude\ teamclaude-dash\ iris-face\
   ├ shared\shims\  claude.cmd · codex.cmd · node.cmd   ← 사용자 PATH에 이 폴더 1개만 추가(관리자 불필요)
   ├ shared\portable-state\teamclaude\ ← 프록시 설정(가능하면; 안 되면 runtime\hosts\…\exceptions.json에 기록)
   └ runtime\hosts\<host-id>\          ← 기계마다 다른 상태
```

- 선택하지 않은 에이전트도 파일은 놓인다(잠듦). `shims`에는 **로그인된 에이전트의 .cmd만** 만들고, 깨울 때 추가한다.
- 이 PC(`<IRIS 루트>`)의 Face·대시보드 위치는 바꾸지 않는다. 빌드 도구가 복사할 때 자리를 바꿔 넣는다.
- 영혼 이름의 한글 허용 여부는 node-pty 실측(8절 ①) 뒤 결정. 실패하면 영문·숫자만.

## 3. 조각 ② — 설치기의 흐름과 화면

### 3-1. 두 겹 구조

겉옷(`.cmd` + `bootstrap.ps1`)은 아무것도 없는 윈도우에서도 도는 최소한만 하고, 속옷(`server.mjs` + 웹 화면)은 동봉 Node 위에서 진짜 일을 한다. PowerShell 몫을 최소화하는 이유 = 한글·인코딩 함정(R-004·R-008·R-010).

### 3-2. 겉옷 — `bootstrap.ps1` (10~20초)

```text
① 위치 확인 — zip 안(임시 폴더)에서 실행했으면 "먼저 압축을 풀어 주세요"
② 무결성 — manifest.json ↔ payload 파일 SHA-256. 다르면 중단
③ 임시 자리 — %LOCALAPPDATA%\IRIS-Installer\ 에 Node만 풀기 (영혼 이름을 모르므로 C:\ 아래엔 아무것도 안 씀)
④ 서버 켜기 — 동봉 node로 installer\server.mjs, 127.0.0.1:3460 준비 확인
⑤ 브라우저 열기 — 기본 브라우저. 검은 창은 최소화("이 창을 닫지 마세요")
```

호출은 `powershell -NoProfile -ExecutionPolicy Bypass -File`이라 실행 정책 문제가 없다. Mark-of-the-Web(인터넷에서 받은 파일 표식)은 ⓓ 복사 단계에서 풀린 파일에 `Unblock-File`로 제거한다(관리자 불필요) — `electron.exe`·`node.exe` 첫 실행 SmartScreen 경고 예방.

### 3-3. 속옷 — 화면 6장 (단일 HTML 마법사, 위 진행 바 + 현재 단계 카드, 새로고침해도 같은 단계)

| 단계 | 화면 | 뒤에서 | 다음 조건 |
|---|---|---|---|
| ⓐ 사전검사 | 체크 3개 ✓/✗ + 실패 이유·할 일 | OS 버전·64비트·여유 공간·`claude.ai`/`chatgpt.com` 도달·기본 브라우저 존재 | 전부 ✓ (인터넷만 ✗면 ⓓ까지 진행 허용) |
| ⓑ 영혼 이름 | 입력 1개 + `C:\NOVA` 미리보기 + 금지 이유 즉시 표시 | 가이드 2-1절 규칙(금지 문자·예약 이름·시스템 폴더·reparse point·NTFS) | 유효 이름. 폴더가 이미 있고 `soul-state.json`이 있으면 "기존 영혼 → 이어서(권장)/다른 이름" 분기(4-6) |
| ⓒ 구독 선택 | ☐ Claude ☐ ChatGPT + 한 줄 설명 | 주도 에이전트·가이드 판 결정 | 1개 이상. 0개면 구독 안내 화면(설치 안 함) |
| ⓓ 복사 | 부품별 프로그레스바 | payload → `_agent\shared\tools` 풀기 · shims · 사용자 PATH · 최소 `soul-state.json` · 영수증 · MOTW 제거 | 전 부품 검증(파일 수·실행 파일 존재·`node -v`·`python -V`·`git --version`·`claude --version`) |
| ⓔ 로그인 | 계정별 카드 "[Claude 로그인] 새 창이 열립니다 → 브라우저에서 로그인 후 돌아오세요" + 상태 | **구독별 CLI 자체 로그인 1회**(`claude`/`codex`를 프록시 변수 없는 별도 콘솔에서 로그인만) → 인증 파일 생성 감지 → TeamClaude가 그 파일에서 계정 가져오기(불가하면 `teamclaude login` 1회 추가) → 상태 폴링 (실측 2026-09-11, 10절 #1) | 선택한 구독 전부 `cli`·`relay` 완료 |
| ⓕ 완료 | "설치가 끝났습니다. 이제 비서가 이어받습니다" + [Face 열기](3초 뒤 자동) + "보안 프로그램이 물으면 허용" | Face 실행(첫 세션 인자) → 데몬 응답·세션 1개 확인 → 영수증 handoff=done → 설치기 종료·임시 폴더 정리 → 바탕화면 바로가기 | Face 데몬 응답 |

- 설치기는 **이름·구독·로그인 3가지만 묻는다.** R/D/P/S/T 구조·아이콘 질문(가이드 5절)은 에이전트 몫이다(같은 질문을 두 번 받지 않게).
- 화면 문구에 TeamClaude라는 이름을 쓰지 않는다("Claude 계정 로그인"). 중계기 이름은 대시보드에서만.
- 회사망이 OAuth를 막으면 ⓔ에서 정확히 알리고 설치는 남긴다. 재실행 시 ⓔ로 바로 간다.
- 로그 = `_agent\setup\package-install.log` 하나(단계·시각·결과, 비밀 없음).

## 4. 조각 ③ — 인수인계: 영수증·최소 표지·첫 요청문·가이드 v10

### 4-1. 설치기가 남기는 세 가지

**① 영수증 `_agent\setup\package-receipt.json`** (schema 1)

```json
{
  "schema": 1,
  "package": { "name": "IRIS", "version": "1.0.0", "built": "2026-09-xx", "guideVersion": "10", "license": "MIT" },
  "soul": { "root": "C:\\NOVA", "name": "NOVA", "createdBy": "package-installer" },
  "choice": { "subscriptions": ["claude", "chatgpt"], "leadAgent": "claude", "guideEdition": "claude" },
  "installed": {
    "node": { "version": "24.x.y", "path": "_agent\\shared\\tools\\node", "sha256": "…", "verified": true },
    "python": { "version": "3.12.x", "path": "…", "verified": true },
    "git": { "version": "2.54.x", "path": "…", "verified": true },
    "claude": { "version": "…", "path": "…", "verified": true, "active": true, "source": "bundled|npm-registry" },
    "codex": { "version": "…", "path": "…", "verified": true, "active": true },
    "teamclaude": { "version": "1.1.16+iris4", "patches": ["server.js", "activity.js", "codex-quota.js", "account-manager.js"], "verified": true },
    "teamclaude-dash": { "verified": true },
    "iris-face": { "version": "2.xx.0", "verified": true }
  },
  "login": { "claude": { "done": true, "at": "…" }, "chatgpt": { "done": true, "at": "…" } },
  "env": { "CLAUDE_CONFIG_DIR": "C:\\NOVA\\_agent\\claude", "CODEX_HOME": "C:\\NOVA\\_agent\\codex", "ANTHROPIC_BASE_URL": "http://127.0.0.1:3456", "pathShim": "C:\\NOVA\\_agent\\shared\\shims" },
  "steps": { "precheck": "done", "name": "done", "choice": "done", "copy": "done", "login": "done", "handoff": "pending" },
  "log": "_agent\\setup\\package-install.log"
}
```

`steps`가 재실행 안전의 근거. 토큰·비밀은 없다. `createdBy`는 `package-installer` | `existing`(4-6).

**② 최소 표지 `soul-state.json`** — 가이드 부록 A 최소 스키마 그대로(UUID · `structureDecision.status`·`iconDecision.status` = `wizard-paused` · `sourceGuide.edition`) + v10 신설 필드 `packageInstall: true`. 이것이 있으면 가이드가 이 폴더를 자기 영혼으로 인정해 `soul-conflict`를 내지 않는다.

**③ 세팅가이드 두 판** — `_setup-guides\`에 둘 다.

### 4-2. 첫 요청문 (Face가 첫 세션에 자동 주입, 파일로 전달)

> `C:\NOVA\_setup-guides\세팅가이드(Claude 실행판)_v10.md`대로 세팅해 줘. 설치 패키지가 먼저 깔아 둔 것은 `C:\NOVA\_agent\setup\package-receipt.json`에 있어. 나한테 물을 것은 일반 문장으로 물어봐.

세 문장 = 계약서 위치 · 인수인계 노트 위치 · Face 노란 카드(AskUserQuestion) 방지. 절대경로를 쓰는 이유 = 영혼 밖에서 실행될 가능성 제거(Face가 폴더를 열어 주므로 이중 안전장치).

### 4-3. Face로 넘기는 방식

설치기 ⓕ가 `launch.mjs --first-session <spec.json>`을 부른다(spec = 폴더·에이전트·모델 기본값·첫 요청문 파일 경로 — 한글·따옴표의 명령줄 깨짐 회피). 데몬은 세션을 만들고 `idle`이 처음 관측될 때 1회 주입(P02 구현계획 1단계 규칙 그대로). 설치기는 `/api/health` 응답 + 세션 1개를 확인한 뒤 `steps.handoff=done`을 쓰고 자기 서버를 끈다.

### 4-4. 가이드 v10 변경점 (원본 `V8-GuideTemplate.md`·`V8-RootAgents.md` 등을 고쳐 `Build-V8Guide.py`로 재생성)

| 절 | v9 | v10 |
|---|---|---|
| 0-2 시작 조건 | Claude Code가 먼저 설치·로그인되어 있어야 함 | 시작 조건 3종 명시: ⓐ 패키지 설치기가 만든 영혼(영수증 있음, **기본**) ⓑ 기존 v7~v9 영혼 ⓒ 손으로 설치한 에이전트 |
| 2-1 영혼 찾기 | 표지 없는 비어 있지 않은 폴더 = `soul-conflict` | 영수증 + 최소 표지 = 패키지 영혼으로 인정. `installed`는 재검증만, 재설치 금지 |
| 2-2 휴대형 홈 | 환경변수 설정 절차 | 설치기가 설정했으면 값 확인만 |
| 8-1 기반 프로그램 | 없으면 설치 | 패키지 영혼에서는 `shared\tools` 동봉본이 정본, 시스템 설치본을 찾지 않음 |
| 8-2·8-3 도구 | 인터넷에서 고정 커밋으로 받기 | **변경 없음**(1.1에서 동봉 검토, 9절) |
| 9 소환기 | `소환하기.cmd` | Face가 있으면 `<영혼> Face.cmd`가 기본 소환기, `소환하기.cmd`는 터미널용 병존 |
| 신설 절 "질문 방식" | — | 세팅 중 질문은 일반 문장·번호 선택지. AskUserQuestion류 도구 사용 안 함 |
| 부록 A | — | `packageInstall` · `sourceGuide.version: "10"` |
| 부록 J | — | v10 변경 기록 |

제안(별도 판단): 부록 H(내장 엔진 약 2만 줄)를 별도 파일로 분리해 본문만 읽게 하는 것 — 가이드 공장 변경이라 v10 범위에 강제하지 않는다.

### 4-5. 가장 큰 위험

가이드가 영수증을 무시하고 재설치하는 것. v10 재생성 뒤 **빈 가상 영혼에 패키지 설치 → 에이전트 실행 → 재설치 0건**을 실측한다(7절 3층 수용 조건).

### 4-6. 기존 영혼을 만났을 때

`C:\<이름>`에 `soul-state.json`이 있으면 설치기는 "기존 영혼 — 이어서 세팅(권장) / 다른 이름" 두 선택만 보인다. 이어서 = 부품은 **없는 것만** 복사(있는 폴더는 절대 덮어쓰지 않음), 영수증 `createdBy=existing` → 가이드는 업데이트 모드(부록 D). 이것이 기존 사용자에게 Face·TeamClaude를 추가 배포하는 경로이자 6-4의 업데이트 경로다.

## 5. 조각 ④ — IRIS-Face 수정점 (P02에서 수행)

### 5-1. 고정 경로 제거 (전수 조사 2026-09-10)

| 파일 | 고정된 것 | 변경 |
|---|---|---|
| `launch.mjs` | 대시보드 `<IRIS 루트>\_agent\claude\tools\teamclaude-dash` | 영혼 루트 기준 `_agent\shared\tools\teamclaude-dash` → 없으면 옛 자리(이 PC 호환) |
| `daemon/agents.mjs` | `CLAUDE_CONFIG_DIR`·`CODEX_HOME` 기본값 `<IRIS 루트>\_agent\…` | 영혼 루트 상대경로 기본값. 환경변수 우선은 그대로 |
| `daemon/doc2pdf.mjs` | 한/글 변환 파이썬 = R07 개발 폴더 venv | 후보에 `_agent\shared\tools\document-mcp\…\.venv` 추가. 없으면 한/글 변환만 불가(현행) |
| `daemon/titler.mjs` | PATH의 `claude.exe` 탐색 | 데몬이 PATH 앞에 shims·동봉 Node를 붙이므로 그대로 |
| `daemon/limits.mjs` | 대시보드 3457 | 변경 없음 |
| `docs/spike-*/*.py` | `<IRIS 루트>` | 배포 제외 |

영혼 루트 = 기존 `findRoot()`(위로 올라가며 `_agent`가 있는 폴더). 새 깊이(`_agent\shared\tools\iris-face`)에서도 찾는지 확인.

### 5-2. 더하는 것 (넷)

1. **첫 세션 인자** `--first-session <spec.json>` (4-3).
2. **자식 세션 환경 주입** — `CLAUDE_CONFIG_DIR`·`CODEX_HOME`·`ANTHROPIC_BASE_URL`·`PATH`(shims + 동봉 Node 앞에)를 영수증에서 읽어 직접 넣는다. 사용자 환경변수는 새 프로세스부터 적용되므로(가이드 2-3절) 설치 직후 첫 세션의 이중 안전장치.
3. **잠든 에이전트 깨우기** — 대시보드의 기존 `＋Claude/Codex 계정` 버튼 뒤 후처리: shims에 `.cmd` 생성 → 영수증 `installed.<agent>.active=true` → 새 작업 화면 선택지에 켬. 새 화면 없음.
4. **버전 원천** — Face `package.json` 버전 = 영수증 `installed.iris-face.version`(빌드 도구가 채움).

### 5-3. 그대로 두는 것·배포에서 빼는 것

| 항목 | 결정 |
|---|---|
| `state\` | 프로그램 폴더 안 그대로(죽은 세션은 "재개 가능" 처리 있음) |
| 🎤 음성(faster-whisper) | 선택 기능. 파이썬 패키지 동봉 안 함. 버튼은 "설치되지 않음 — 에이전트에게 설치 요청" 안내 |
| 자동 조합 판정·라우터 | 이미 폐기(v0.2), 변경 없음 |
| `docs\`·`_archive\`·`state\`·`.git`·`*.pdb` | 배포 제외 |
| `node_modules\`(Electron·node-pty·xterm) | 동봉. 빌드 임시 폴더에서 `npm ci`로 새로 설치(개발 잔재 배제). node-pty ABI = 동봉 Node 검사 |

### 5-4. 디자인 통일

설치기 HTML은 Face `app/style.css` 맨 위 변수 블록(`--base`·`--ink-2`·`--iris`·`--mono` 등)을 **복사**한다(설치기가 Face보다 먼저 떠야 하므로 공용 파일 의존 불가). 빌드 도구가 두 블록의 동일성을 검사한다.

이 PC의 Face는 "패키지 자리 → 없으면 옛 자리" 순서라 그대로 돈다. 이 PC를 패키지 구조로 옮기지 않는다.

## 6. 조각 ⑤ — 빌드 도구(공장)와 검증(검사소)

### 6-1. 위치와 언어

이 프로젝트(P03). `installer\` · `build\`(collect·sanitize·manifest·pack·build) · `verify\`(static·reproduce·e2e-checklist.md) · `patches\teamclaude\`(앵커→치환 규칙, 이 PC의 `teamclaude-local-patches-20260907.zip` README에서 옮김) · `lock.json` · `_build\cache\`(내려받은 원본) · `_build\out\`(완성 zip). 언어 = **Node(ESM) 하나** — 설치기·Face·대시보드와 같고, 지문 코드(`lib/manifest.mjs`)를 설치기와 공유한다.

### 6-2. 공장 4단계 `node build/build.mjs`

```text
① 수집(collect) — lock.json대로. 인터넷에서 받는 것은 SHA-256이 맞을 때만 캐시 저장
② 정화(sanitize) — 개인정보·고정경로·비밀파일이 있으면 즉시 중단
③ 지문(manifest) — 전 파일 SHA-256 + 버전표 → manifest.json
④ 포장(pack) — 최대 압축 zip + .sha256. 이름 IRIS-Setup_v<패키지버전>_<YYYY-MM-DD>.zip (영문 고정 — 8-1절)
```

| 부품 | 출처 | 특별 처리 |
|---|---|---|
| Node 24 | nodejs.org 공식 zip | SHA 대조 |
| Claude Code·Codex | npm 레지스트리 `npm pack <이름>@<버전>` | integrity 기록. Claude Code는 6-3 결과에 따라 동봉 또는 설치 시 내려받기 |
| TeamClaude | `npm install -g --prefix` 로 pristine @karpeleslab/teamclaude@<버전> 설치 → 라이브 설치본 기준 패치 규칙(`patches/teamclaude/rules.json`: replace + 신규 파일 create) 적용 → zip | `must_replace`: 앵커가 정확히 1번 없으면 **중단**(공식판 변경 신호). 회귀 테스트 22개 재실행 |
| IRIS-Face | 이 PC P02 | `docs`·`_archive`·`state`·`.git` 제외. `node_modules`는 빌드 임시 폴더에서 `npm ci` |
| IRIS 메신저 모듈(1.4.0~) | P04 GitHub 릴리스 `iris-messenger-v<ver>.zip` | SHA 대조. 설치 시 Face 자체 설치기(`face\daemon\modinstall.mjs` — 경로·지문·서명 검사)로 `face\modules\messenger`에 심고 `.official` 표시. 사용자 결정 2026-09-14: 기본 설치 |
| 대시보드 | `_agent\claude\tools\teamclaude-dash` | `design\` 제외 |
| Python 임베디드 + PyYAML 휠 | python.org · PyPI | SHA 대조 |
| MinGit | git-for-windows 공식 | SHA 대조 |
| 세팅가이드 v11(1.4.0~; 1.3.x는 v10) | `_setup-guides\…_v11_…_최신.md` | 파일명 v11 = 본문 버전 줄 검사. 가이드 자체는 가이드 공장(`Build-V8Guide.py`) 몫. v11 = 패키지 영혼 전체 추출 예외·아이콘 미리보기·메신저 로그인 마무리 |

**설치 시 미리 쓰는 설정(1.4.0~, 사용자 결정 2026-09-14 "권한 최대").** ⓓ 복사 단계 3c에서 `installer\lib\firstrun.mjs` `seedPermissions()`가 Claude `_agent\claude\settings.json`에 `permissions.defaultMode = "bypassPermissions"`·`skipDangerousModePermissionPrompt = true`, Codex `_agent\codex\config.toml` 맨 위에 `approval_policy = "never"`·`sandbox_mode = "danger-full-access"`를 **없는 키만** 더한다(있는 값은 건드리지 않음; Codex 키는 `[table]` 위에 있어야 최상위 설정이라 파일 머리에 넣는다). 첫 세션 spec(`first-session.json`)에도 같은 값을 `permission`·`approval`·`sandbox`로 명시하고 Face v2.59+ `launch.mjs`가 `POST /api/sessions`에 그대로 넘긴다. 가이드 1-3절은 같은 값을 다시 확인만 하게 된다.

**정화가 잡는 것**(하나라도 있으면 zip을 만들지 않음): ① 비밀 파일 패턴(`.env`·`secrets\`·`auth.json`·`teamclaude.json`·`*.token`·`credentials*`) ② 개인정보 문자열(v9 유출 검사 목록 재사용 + 이메일 정규식·저자 개인 계정 문자열(로컬 규칙 파일 `build/sanitize-local.json`, gitignore됨) + 사용자 프로필 하위 경로 정규식) ③ 개발 PC 고정 경로(예외 없음 — Face의 옛 자리 호환은 영혼 루트 상대경로로 구현하므로 리터럴이 남지 않는다) ④ 압축 전 600MB 초과 경고. **Fix round 1(C1/C2) 이후:** 추적 파일에는 개인 문자열을 한 글자도 두지 않는다 — 규칙은 tracked `build/sanitize-rules.json`(일반 규칙만) + gitignore된 `build/sanitize-local.json`(저자 개인 문자열) 두 층으로 나뉘며, `build/rules.mjs`의 `loadRules()`가 병합한다. 추가로 정화 게이트 자체가 `git ls-files` 기준 저장소 추적 파일 전체를 스캔하는 자기 검사(⑦)를 검사소 3층에 포함한다(구 — 정화는 빌드 산출물만 스캔해 저장소 자신은 스캔하지 않는 사각지대가 있었음).

### 6-3. 🔴 재배포 권리 확인 (구현 첫날)

| 부품 | 허가서 | 동봉 |
|---|---|---|
| Node.js·Electron·xterm·TeamClaude | MIT | 가능(사본 동봉) |
| Python | PSF | 가능 |
| Git(MinGit) | GPLv2 | 가능(원본 그대로 + 사본) |
| Codex | Apache-2.0 | 가능 |
| **Claude Code** | Anthropic 상업 약관(비공개) | **확인 필요.** 불허면 설치기 ⓓ에서 동봉 Node의 npm으로 **자동 내려받기**(사용자 단계 0 유지). npm이 막힌 망이면 정확한 문구로 안내 |

### 6-4. 검사소 3층

| 층 | 무엇을 | 언제 |
|---|---|---|
| 1 정적 `verify/static.mjs` | manifest ↔ 파일 · 정화 0건 · lock 부품 전부 존재 · Face 변수 블록 = 설치기 변수 블록 · node-pty ABI = 동봉 Node | 매 빌드 |
| 2 재현성 `verify/reproduce.mjs` | 같은 lock으로 두 번 빌드 → manifest 지문 전부 동일(시각 정규화) | 매 빌드 |
| 3 실행 `verify/e2e-checklist.md` | 새 컴퓨터에서 완주(7절 수용 조건) | 배포 전 필수, 사람이 대장에 기록 |

**"새 컴퓨터" 3단계**(이 PC는 Windows 11 Home → Sandbox·Hyper-V 없음): ① 이 PC의 새 로컬 사용자 계정(매 빌드, 빠름 — 설치기가 시스템 PATH를 무시하는지 확인) ② VirtualBox + Windows 11 평가판(배포 전마다, 깨끗한 스냅샷 보관해 매 시험 5분) ③ 베타 테스터 2~3명 실제 PC(첫 공개 전 1회, 회사·학교 PC 1대 포함).

### 6-5. 원칙

버전 3종 분리(패키지 ≠ Face ≠ 가이드, manifest가 조합 기록) · 공장 코드 자체에 단위 시험(정화 규칙이 잡는지, 앵커 불일치가 멈추는지 — v9 `Test-OntologyPackage.py` 13개 선례) · 결과는 `docs\검증기록.md`에 프로그레스바 형식.

## 7. 수용 조건 (3층 완주 — 전부 ✓여야 출고)

```text
□ zip 풀고 IRIS-설치.cmd 두 번 클릭 → 브라우저 설치 화면이 뜬다
□ 사전검사 3개 ✓ · 이름 · 구독 선택 · 복사(전 부품 검증 ✓) · 로그인 완료
□ Face 창이 뜨고 첫 세션에 첫 요청문이 자동으로 들어간다
□ 에이전트가 영수증을 읽고 재설치 0건 (로그로 확인)
□ 에이전트가 세팅을 끝까지 마치고 가이드 10절 완료 보고를 낸다
□ 재부팅 → 바탕화면 아이콘으로 Face가 다시 뜬다 · 프록시가 자동 기동된다
□ 설치기 재실행 → "기존 영혼" 분기 → 아무것도 덮어쓰지 않는다
□ 한글 영혼 이름(C:\비서)으로 위 전부 재시험 (실패하면 영문 제한 확정)
□ 정화 0건 · 재현성 통과 · 이 PC의 Face·대시보드·가이드 원본 무접촉(해시 전후 동일)
```

## 8. 조각 ⑥ — 배포·업데이트·첫 공개 순서

### 8-1. 배포 선반

**릴리스 첨부 이름은 영문 고정** `IRIS-Setup_v<판>_<YYYY-MM-DD>.zip`(+`.sha256` 안의 이름도) — GitHub가 첨부 이름의 한글을 지우기 때문이다(v1.2.0 실측). zip **안**의 `IRIS-설치.cmd`는 그대로 한글이다.

GitHub Pages(홈페이지)는 파일당 100MB 제한 → zip을 못 놓는다. **GitHub Releases**(파일당 2GB, 버전별 고정 주소, `.sha256` 병기)를 주 통로로, 홈페이지에는 `/download` 페이지 하나(항상 최신 릴리스로 연결, 주소 불변). 구글 드라이브는 예비(100MB 초과 시 검사 불가 경고), USB는 연수용 병행(같은 zip, 별도 판 없음). 공개 저장소(MIT, IRIS)에 P03과 P02 소스를 둔다 — 공개 전 정화 규칙을 저장소에도 돌린다.

### 8-2. 새 버전

```text
우리 — 원본 수정 → 빌드 → 검사소 3층 → Release v1.x.0 (zip + .sha256 + 변경 요약)
사용자 — Face 상단 "새 버전" 배지(하루 1회 릴리스 목록 확인, 설정에서 끔 가능, 첫 실행 때 한 줄 고지)
사용자 — 배지 → /download → 새 zip → IRIS-설치.cmd (처음과 같은 동작)
설치기 — "기존 영혼" 분기 → 영수증 버전 비교 → 바뀐 부품만 교체(이전 판 .prev 보존) → 영수증 갱신
```

사용자 자료(R/D/P·기억·설정·시크릿)는 업데이트가 절대 만지지 않는다. Face·프록시 실행 중이면 끄지 않고 "다음 실행부터"로 표시. 에이전트 자체 업데이트는 사용자 규칙(2026-09-10, 자동 업데이트 켬)을 따르되 npm 포터블 설치에서 Claude Code 자체 업데이터가 우리 폴더에 쓸 수 있는지는 실측(안 되면 Face가 `npm update`를 대신 돈다).

### 8-3. 문제 보고

Face 정보창·설치기 실패 화면에 **[문제 보고 파일 만들기]** — 설치 로그·영수증·데몬 로그·버전표를 비밀 없이 zip 하나로 바탕화면에. 대화 내용·토큰·자료는 넣지 않는다.

### 8-4. 첫 공개까지의 순서 (구현 계획의 뼈대)

```text
① 실측 3건 — 로그인 횟수(프록시 경유 시 claude 자체 로그인 필요 여부) · node-pty(ABI·한글 경로) · Claude Code 재배포 가능 여부
② P03 설치기 — bootstrap.ps1 · server.mjs · ui (이름·구독·복사·로그인·인수인계)
③ Face 수정(5절)
④ 가이드 v10 — 원본 수정 → Build-V8Guide.py → Test 통과
⑤ 빌드 도구(6절) + 정적·재현성 검사
⑥ 검증 3층 — 새 계정 → VirtualBox → 완주 9항목 ✓
⑦ 공개 저장소 + Release v1.0.0 + 홈페이지 /download
⑧ 베타 2~3명 → 문제 보고 파일로 수정 → v1.0.x
⑨ 공개 — 연수·책에서 "zip 받아 두 번 클릭" 한 줄
```

## 9. 범위 밖 · 다음 판(1.1) 후보

- 가이드 8-2절 도구(Superpowers·MCP 7종·k-skill)의 동봉 — 1.0은 에이전트가 인터넷에서 받음(막히면 `pendingCapabilities`로 기록되고 세팅은 완료). 1.1에서 lock에 넣어 동봉(약 +150MB).
- 가이드 부록 H 분리(본문만 읽기) — 가이드 공장 판단.
- Face 상단 "새 버전" 배지(8-2)와 [문제 보고 파일 만들기] 버튼(8-3) — 1.0에서는 `/download` 수동 확인과 로그 경로(`_agent\setup\package-install.log`·Face `state\daemon.log`) 안내로 대체(구현계획 자체 점검에서 이동).
- 음성 입력 패키지 동봉 — 선택 기능으로 유지.
- 맥·리눅스 — 이 패키지는 Windows 전용(가이드와 동일).

## 10. 열린 항목 (구현 첫날 실측으로 닫는다)

| # | 항목 | 갈림 |
|---|---|---|
| 1 | TeamClaude 로그인만으로 `claude`가 동작하는가 | **실측 2026-09-11: 아니다.** `claude`는 자체 인증 파일(`.credentials.json`)을 요구하고, `ANTHROPIC_BASE_URL`이 설정된 동안은 `/login` 자체가 "isn't available in this environment"로 비활성이다. → ⓔ 흐름 확정: 구독별로 **CLI 자체 로그인 1회**(프록시 변수를 비운 별도 콘솔, 프롬프트 실행 없음) → 인증 파일 생성 감지 → TeamClaude가 그 파일에서 계정 **가져오기(importFrom)**. 가져오기가 불가하면 `teamclaude login` 브라우저 1회 추가. 영수증 `login.<provider>.cli`·`login.<provider>.relay` 두 값으로 기록 |
| 2 | node-pty: 동봉 Node ABI 일치 · 한글 영혼 경로 · 압축 해제 후 실행 | **실측 2026-09-11: 전부 통과**(node-pty 1.1.0은 `prebuilds\win32-x64\pty.node` 동봉, `npm ci` 2초·컴파일 없음, `C:\비서시험`에서 세션 생성 200) → 한글 영혼 이름 허용 |
| 3 | Claude Code npm 묶음 재배포 가능 여부 | **실측 2026-09-11: 불허**(`LICENSE.md` "© Anthropic PBC. All rights reserved") → 설치 시 자동 내려받기(6-3) 확정. Codex Apache-2.0·TeamClaude MIT는 동봉 |
| 4 | TeamClaude 설정 경로를 영혼 안으로 옮길 수 있는가 | **확인 2026-09-11(Task 6): 가능.** pristine `src/config.js`의 `getConfigPath()`가 `TEAMCLAUDE_CONFIG` 환경변수를 먼저 읽는다 → 설치기가 `TEAMCLAUDE_CONFIG=<root>\_agent\shared\portable-state\teamclaude\teamclaude.json`을 사용자 환경변수·심·manage 스크립트에 넣는다. `exceptions.json` 불필요 |
| 5 | Claude Code 자체 업데이터가 포터블 폴더에서 동작하는가 | 안 되면 Face가 대행 |

## 11. 개정 — 2026-09-13 배포 전 시나리오 점검 (사용자 확정)

이 절은 위 1~10절의 본문을 고쳐 쓰지 않고, 사용자와 단계별 점검(검증기록 14절)에서 **확정된 변경**만 적는다. 충돌하면 이 절이 우선한다.

1. **영혼 폴더는 `C:\IRIS`로 절대 고정.** 3-3절 ⓑ "영혼 이름"은 "설치 위치 확인"이 된다 — 입력 없음, ⓐ 통과 시 자동, `C:\IRIS`가 IRIS가 아닌 자료로 차 있을 때만 막히고 「다시 확인」. 4-6절의 이름 분기·한글 이름 허용은 사용자에게 노출되지 않는다(개발 PC 연습용 환경변수 `IRIS_INSTALLER_SOUL_NAME`만 남김). 그에 따라 "설치기는 이름·구독·로그인 **3가지만** 묻는다" → **구독·로그인 2가지**.
2. **이름:** IRIS = 시스템 전체, IRIS-Face = 창 껍데기의 내부 명칭. 화면·바로가기·설명에는 "IRIS"만. 바탕화면 바로가기 = `IRIS.lnk`(설명 "IRIS"). 루트 소환기는 세팅가이드 v10 9절 호환을 위해 `IRIS Face.cmd`로 두고, 가이드 v11 때 `IRIS.cmd`로 개명한다.
3. **ⓔ 로그인 명령:** Claude Code는 `claude auth login --claudeai`(2.1.x에 `claude login` 없음), Codex는 `codex login`. 검은 창은 `cmd /c … || pause`(성공 시 저절로 닫힘, 실패 시 열린 채 오류 표시). 「다시 열기」는 2분 뒤.
4. **ⓓ 「첫 실행 준비」 단계 추가:** 설치기가 `_agent\claude\.claude.json`(onboarding 완료·`C:/IRIS` 신뢰)과 `_agent\codex\config.toml`(`trust_level`)을 **없을 때 만들고 있으면 빠진 키만 더한다.** 목적 = IRIS 창의 첫 화면이 영어 마법사가 아니라 세팅 대화이게. 4-1절 최소 표지의 `sourceGuide.version`은 동봉 가이드 판("10").
5. **ⓐ 오프라인:** 인터넷 없이 진행 가능한 범위는 ⓒ까지다(Claude Code는 ⓓ에서 npm으로 내려받으므로). 3-3절 ⓐ의 "인터넷만 ✗면 ⓓ까지 허용"은 화면 문구에서 사실대로 고쳤다(진행 자체는 막지 않음).
6. **zip 포장:** 항목 이름을 UTF-8 표식과 함께 쓴다(`tar --options zip:hdrcharset=UTF-8`) — 비한국어 윈도우에서도 `IRIS-설치.cmd`·가이드 파일명이 깨지지 않게.
7. **3-1절 ⓕ의 "설치 창 자동 닫힘"**은 서버 종료로 정정한다(브라우저 탭은 사용자가 닫음).

## 12. 업데이트 (2026-09-14) — 패키지 v1.3.0

**정본은 이 문서가 아니다.** Face·설치기·적용기·릴리스 도구·홈페이지가 함께 지키는 계약은 P02 `docs\설계-업데이트-2026-09-14.md`에 있고, 이 절은 그중 **P03이 맡는 몫**만 적는다. 충돌하면 그 문서가 이긴다. 8-2절의 "새 버전" 흐름이 여기서 손 클릭 없는 흐름으로 바뀐다.

1. **적용기 `updater\apply.mjs`** (새 부품 `updater` → `<root>\_agent\shared\tools\updater\`). Face 데몬은 자기가 실행 중인 폴더를 자기가 바꿀 수 없으므로, 데몬이 `plan.json`을 쓰고 이 프로그램을 분리 실행한 뒤 스스로 꺼진다. 부르는 법은 `node apply.mjs --plan <plan.json>`과 위치 인자 `node apply.mjs <plan.json>` **둘 다** 받는다(데몬이 지금 쓰는 형태). 절차는 ⓪ `plan.schema`가 1이 아니면 중단 → ① 데몬 PID와 포트가 **둘 다** 사라질 때까지 최대 90초(안 꺼지면 아무것도 바꾸지 않음) → ② 항목의 `dir`이 `<root>\_agent\shared\downloads` 아래인지 확인 → ③ `tools\face`를 `.prev`로 밀어내고(EBUSY·EPERM은 100ms×20 재시도) 새 폴더를 **복사**해 앉히고 → ④ `package-lock.json`이 같으면(줄바꿈 방식·줄 끝 공백 차이는 무시) 이전 `node_modules`를 옮기고 다르면 동봉 npm으로 `ci`(빌드 공장의 `npmCi`와 같은 전체 의존성 — Face의 `electron`이 devDependencies라 `--omit=dev`를 쓰면 창이 못 뜬다, 2026-09-14 실사용 시험에서 발견)(실패하면 통째로 되돌림) → ⑤ 사용자 것인 `state\`·`modules\`를 새 폴더로 옮기고 → ⑥ 영수증 `installed.face`를 원자적으로 갱신한 뒤 → ⑦ 받은 폴더를 지우고 `update-result.json`을 쓰고 → ⑧ `launch-hidden.vbs`로 IRIS 창을 다시 연다. `package`(구조판) 항목이 있으면 2번의 자동 모드에 통째로 넘기고 끝낸다(그 인계가 **실패하면** 적용기가 창을 대신 연다).
   - **받은 폴더는 옮기지 않고 복사한다** — 되돌릴 때 검증까지 끝난 그 내려받기가 함께 사라지면 재시도에 다시 내려받아야 하기 때문. 성공해서 되돌릴 것이 없어진 뒤에야 지운다.
   - **새 Face 폴더에 `package-lock.json`이 없으면 `no-package-lock`으로 즉시 거절**한다(아무것도 건드리지 않음). `npm ci`는 그 파일 없이는 돌지 않고, 대신 옛 `node_modules`를 옮기면 서로 다른 두 판의 반쪽을 붙인 Face가 되기 때문이다.
   - **모든 항목은 try/catch 안에서 돈다** — 예상 못 한 실패가 나도 `update-result.json`과 재실행은 반드시 수행한다.
   - `.prev` 회전·`moveDir`·`carryOver` 세 함수는 `installer\lib\install.mjs`의 것과 **같은 규칙의 복제본**이며(단독 배치라 import 불가), `tests\updater.test.mjs`의 대조 시험 두 개가 두 벌을 묶어 둔다.
2. **자동 업데이트 모드** — `IRIS-설치.cmd --auto` 또는 `IRIS_INSTALLER_AUTO=1`. **영수증이 있는 PC에서만** 발동하고, 없으면 평소의 여섯 단계 마법사로 그대로 흐른다. 어느 영혼을 고칠지는 적용기가 함께 넘기는 `IRIS_INSTALLER_SOUL_NAME`(= `plan.root`의 마지막 폴더 이름)이 정한다 — 설치기가 `C:\IRIS`를 가정하지 않는다. 동작 = 준비 확인 → 영수증 판과 비교해 **바뀐 부품만** 교체(`.prev` 보존, 사용자 자료 무접촉 — `install()`의 기존 건너뛰기 판정 그대로) → 로그인 생략 → 영수증 갱신 → IRIS 창 다시 열기(첫 요청문 없는 평범한 실행기) → 설치기 종료. 화면은 카드 한 장으로 진행만 보이다가 "업데이트가 끝났습니다".
   - **부품 슬롯 교체 때 사용자 폴더를 함께 옮긴다**(`LAYOUT.<부품>.carryOver`, 지금은 face의 `state`·`modules`). 그 두 폴더는 face zip에서 빠져 있어 교체하면 `.prev`에만 남았고, 그대로면 업데이트 뒤 세션·설정·모듈이 전부 사라진다. 옮기는 시점은 **검증을 통과한 뒤** — 그 전까지는 되돌리기가 새 폴더 삭제이므로, 사용자 폴더를 먼저 넣으면 되돌리기가 곧 자료 삭제가 된다. 새 판이 같은 이름을 이미 갖고 있으면 덮지 않고 영수증에 `carryFailed`로 적는다.
3. **산출물 이름 영문** `IRIS-Setup_v<판>_<YYYY-MM-DD>.zip` (8-1절).
4. **판** `lock.json package.version` = `1.3.0`.

검사 = `tests/updater.test.mjs`(적용기: 교체·보존·`.prev`·영수증·결과 파일·npm 호출·롤백·대기 시간초과) · `tests/auto.test.mjs`(자동 모드: 자격 판정·로그인 생략·창 다시 열기·실패 처리) + 기존 정적 ①~⑨·재현성.

